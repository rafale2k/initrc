## editors/.nanorc (Hardcore Monokai Template)
set linenumbers
set softwrap
set tabsize 4
set tabstospaces
set constantshow
set smarthome
set autoindent

# --- Monokai Interface Colors ---
# 背景色を意識したコントラストの強い配色
set titlecolor brightwhite,black
set statuscolor brightwhite,black
set errorcolor brightwhite,red
set selectedcolor black,brightmagenta
set numbercolor brightcyan      # 行番号はMonokaiブルー
set keycolor brightmagenta      # ショートカットはMonokaiピンク
set functioncolor brightyellow  # 関数はMonokaiイエロー

# --- ハイライト設定 ---
# 1. 標準のハイライト
#include "/usr/share/nano/*.nanorc"

# 2. 外部のハイライトを一括読み込み
# (ここに Monokai 用のカラー定義が含まれていることを期待)
include "DOTFILES_REAL_PATH/editors/nano-syntax-highlighting/*.nanorc"

# 3. 追加のどぎつい配色おまじない (もしあれば個別に指定)
include "DOTFILES_REAL_PATH/editors/my-themes/monokai.nanorc"
